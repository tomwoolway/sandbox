{
    "requestId": "{{ request_id }}",
    "payload": {
        "commands": [{
            "ids": {{ device_ids }},
            "status": "SUCCESS",
            "states": {
                "on": {{ device_state }},
                "online": true,
                "currentFanSpeedSetting" : "{{ current_fan_speed }}"
            }
        }]
    }
}
